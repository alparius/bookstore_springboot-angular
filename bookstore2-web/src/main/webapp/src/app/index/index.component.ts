import {Component} from '@angular/core';


@Component({
    selector: 'app-index',
    templateUrl: './index.component.html',
    styleUrls: ['./index.component.css']
})
export class IndexComponent {
    title = 'Online Bookstore';

    constructor() {
    }
}

/*
const - in reducers
lambda bodies with arg => statement
default parameters in function headers
`${parameters} in strings`
property shorthands: obj = { x, y }
export values
oop type class definitions
static class members
iterators: for (let ob of list)
date formatting
 */
